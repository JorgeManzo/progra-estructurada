#include <stdio.h>

int main()
{
    int num;
    printf("ingresa el numero\n", 160);
    scanf("%d", &num);
    if (num % 2 != 0)
    {
        printf("Es impar");
    }

    return 0;
}
